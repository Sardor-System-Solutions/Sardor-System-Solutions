# Source screenshots

Original captures, with the browser window still visible. The versions the
site actually serves live in `public/work/` — cropped below the browser
chrome and resampled to 2000px wide.

To regenerate them after replacing a capture here, crop off everything above
the first row that is not predominantly dark (the tab strip, address bar and
bookmarks bar), then resize to 2000px wide.
